name: core
description: Shared configuration, error types, logging, and utilities for the Twilight platform.
publish_to: none
version: 0.1.0

resolution: workspace

environment:
  sdk: '>=3.5.0 <4.0.0'

dev_dependencies:
  test: ^1.25.0
