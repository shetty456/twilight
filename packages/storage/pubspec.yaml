name: storage
description: Key-value storage abstraction for the Twilight platform.
publish_to: none
version: 0.1.0

resolution: workspace

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  shared_preferences: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
