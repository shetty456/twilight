name: auth
description: Authentication abstractions, session handling, and Riverpod providers for the Twilight platform.
publish_to: none
version: 0.1.0

resolution: workspace

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  core:
    path: ../core

dev_dependencies:
  flutter_test:
    sdk: flutter
